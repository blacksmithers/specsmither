"""SpecSmither planning lifecycle — the agent surface.

Submodules are imported directly (e.g. ``from specsmither.lifecycle.state_machine
import PHASE_ORDER``); this package root is intentionally bare.
"""
